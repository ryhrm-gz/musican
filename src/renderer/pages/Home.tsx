import { Box, Divider } from "@mantine/core";
import { HomeButtons } from "../components/HomeButtons";

export const Home = () => {
  return (
    <Box>
      <HomeButtons />
      <Divider />
    </Box>
  );
};
